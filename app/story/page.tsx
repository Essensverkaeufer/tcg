import { AppShell } from "@/components/AppShell";
import { StoryMapClient } from "@/components/story/StoryMapClient";

export default function StoryPage() {
  return (
    <AppShell>
      <StoryMapClient />
    </AppShell>
  );
}
